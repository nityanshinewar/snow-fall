class Snowfall{

    constructor(width,height,radius){
     var options={
         restitution:1,
         friction:0,
         isStatic=true
     }
    
    }
}
display() {
var pos= this.body.position;
var angle=this.body.angle;

imageMode(CENTER);
ellipseMode(RADIUS)

}